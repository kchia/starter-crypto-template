// TO DO: import any third party or internal libraries needed

export default function CollectiblesList() {
  // TO DO: Call hooks (e.g., useState(), useErrorHandler(), useHistory(), etc.) at the top of the component

  // TO DO: Use useEffect() to load the list of collectibles on component mount. Use AbortController to abort fetches on component dismount. Propagate errors to the error boundary defined in the Layout component.

  // TO DO: Add rendering logic for displaying the collectibles table. Display a loader while the fetch call is pending. Wrap the table in an ErrorBoundary component.

  return <div>CollectiblesList</div>;
}
