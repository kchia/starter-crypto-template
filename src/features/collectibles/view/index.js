// TO DO: import any third party or internal libraries needed

export default function CollectibleView() {
  // TO DO: Call hooks (e.g., useState(), useErrorHandler(), useHistory(), etc.) at the top of the component

  // TO DO: Use useEffect() to load a collectible by a given id on component mount. Use AbortController to abort fetches on component dismount. Propagate errors to the ErrorBoundary defined in the Layout component.

  // TO DO: Add rendering logic for displaying the collectible card. Display a loader while the fetch call is pending. Wrap the collectible card in an ErrorBoundary component.
  return <div>CollectibleView</div>;
}
